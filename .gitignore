node_modules/
.env
.ditto-config.json
.DS_Store
*.tgz
# root dev config has an absolute path — ignore it; the plugin's plugin/.mcp.json IS tracked
/.mcp.json

# Local-only — workspace-specific, never publish
translation-assets/
.claude/

# The smoke test talks to the real workspace, so it stays local. The detector
# regression suite is pure (no network, no credentials) and IS tracked.
test/*
!test/variablise-detection-test.mjs
